import config from "./config";

export default {
  currentFrame: null,
  currentKnot: null,
  userLine: null,
  gridLines: null,
  mouseTracker: null,
  canvasWidth: 0,
  canvasHeight: 0,
  columns: 0,
  rows: 0,
  squareSize: config.targetSquareSize,
};
