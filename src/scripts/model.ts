import config from "./config";

export default {
  frame: null,
  knot: null,
  userLine: null,
  gridLines: null,
  mouseTracker: null,
  canvasWidth: 0,
  canvasHeight: 0,
  columns: 0,
  rows: 0,
  squareSize: config.targetSquareSize,
};
