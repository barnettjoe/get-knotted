var config = {
    graphRows: 6,
    graphCols: 4,
    squareHeight: 200,
    graphLine: {
        stroke: "black",
        strokeWidth: 5 // keeping stroke widths small avoids annoying edgecase behaviour where mouse is on the grid itself
    },
    grid: {
        fill: "none",
        stroke: "#ff00ff",
        strokeWidth: 5,
        strokeOpacity: 1
    },
    graph_spacing: 200
};